@extends(''layouts.app'')

@section(''content'')
<div class="min-h-screen bg-gradient-to-br from-slate-950 via-gray-900 to-slate-950 text-white py-12">
    <div class="container mx-auto px-4 max-w-5xl">
        
        <!-- Header with Back Button -->
        <div class="flex items-center justify-between mb-8 border-b border-cyan-400/20 pb-4">
            <h1 class="text-4xl font-extrabold text-white tracking-tight">
                Support <span class="text-cyan-400">Details</span>
            </h1>
            <a href="{{ route(''admin.support.index'') }}" 
               class="inline-flex items-center gap-2 bg-slate-800 hover:bg-slate-700 px-5 py-2.5 rounded-full text-sm font-semibold transition-all shadow-lg border border-cyan-400/30">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M9.707 16.707a1 1 0 01-1.414 0l-6-6a1 1 0 010-1.414l6-6a1 1 0 011.414 1.414L5.414 9H17a1 1 0 110 2H5.414l4.293 4.293a1 1 0 010 1.414z" clip-rule="evenodd"/>
                </svg>
                Back to All Messages
            </a>
        </div>

        @if(session(''success''))
            <div class="bg-green-600/20 border-l-4 border-green-400 text-green-300 p-4 rounded-xl mb-8 shadow-lg">
                <div class="flex items-center">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 mr-3" viewBox="0 0 20 20" fill="currentColor">
                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                    </svg>
                    <span class="font-semibold">{{ session(''success'') }}</span>
                </div>
            </div>
        @endif

        <!-- Message Card -->
        <div class="bg-slate-900 rounded-3xl p-8 border border-cyan-400/30 shadow-2xl mb-8">
            
            <!-- Header with Status and Priority -->
            <div class="flex flex-wrap items-center gap-4 mb-6 pb-6 border-b border-slate-700">
                <h2 class="text-2xl font-bold text-cyan-300 flex-1">{{ $message->subject }}</h2>
                
                <!-- Status Badge -->
                <span class="px-4 py-2 rounded-full text-sm font-semibold
                    @if($message->status === ''open'')
                        bg-red-900/50 text-red-300 border border-red-700
                    @elseif($message->status === ''in_progress'')
                        bg-yellow-900/50 text-yellow-300 border border-yellow-700
                    @else
                        bg-green-900/50 text-green-300 border border-green-700
                    @endif
                ">
                    {{ ucfirst(str_replace(''_'', '' '', $message->status)) }}
                </span>

                <!-- Priority Badge -->
                <span class="px-3 py-1 rounded-lg text-xs font-bold
                    @if($message->priority === ''high'')
                        bg-red-700 text-red-100
                    @elseif($message->priority === ''medium'')
                        bg-yellow-700 text-yellow-100
                    @else
                        bg-gray-700 text-gray-200
                    @endif
                ">
                    {{ ucfirst($message->priority) }} Priority
                </span>
            </div>

            <!-- Message Details Grid -->
            <div class="grid md:grid-cols-2 gap-6 mb-6">
                <div class="space-y-3">
                    <div class="flex items-center gap-3">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-cyan-400" viewBox="0 0 20 20" fill="currentColor">
                            <path fill-rule="evenodd" d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z" clip-rule="evenodd"/>
                        </svg>
                        <div>
                            <p class="text-xs text-slate-400 uppercase tracking-wide">Submitted By</p>
                            <p class="text-white font-semibold">{{ $message->user->name }}</p>
                        </div>
                    </div>

                    <div class="flex items-center gap-3">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-cyan-400" viewBox="0 0 20 20" fill="currentColor">
                            <path d="M2.003 5.884L10 9.882l7.997-3.998A2 2 0 0016 4H4a2 2 0 00-1.997 1.884z"/>
                            <path d="M18 8.118l-8 4-8-4V14a2 2 0 002 2h12a2 2 0 002-2V8.118z"/>
                        </svg>
                        <div>
                            <p class="text-xs text-slate-400 uppercase tracking-wide">Email</p>
                            <p class="text-white">{{ $message->user->email }}</p>
                        </div>
                    </div>
                </div>

                <div class="space-y-3">
                    <div class="flex items-center gap-3">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-cyan-400" viewBox="0 0 20 20" fill="currentColor">
                            <path fill-rule="evenodd" d="M6 2a1 1 0 00-1 1v1H4a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V6a2 2 0 00-2-2h-1V3a1 1 0 10-2 0v1H7V3a1 1 0 00-1-1zm0 5a1 1 0 000 2h8a1 1 0 100-2H6z" clip-rule="evenodd"/>
                        </svg>
                        <div>
                            <p class="text-xs text-slate-400 uppercase tracking-wide">Submitted On</p>
                            <p class="text-white">{{ $message->created_at->format(''M d, Y - h:i A'') }}</p>
                        </div>
                    </div>

                    @if($message->event_id && $message->event)
                        <div class="flex items-center gap-3">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-cyan-400" viewBox="0 0 20 20" fill="currentColor">
                                <path fill-rule="evenodd" d="M5 3a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2V5a2 2 0 00-2-2H5zm9 4a1 1 0 10-2 0v6a1 1 0 102 0V7zm-3 2a1 1 0 10-2 0v4a1 1 0 102 0V9zm-3 3a1 1 0 10-2 0v1a1 1 0 102 0v-1z" clip-rule="evenodd"/>
                            </svg>
                            <div>
                                <p class="text-xs text-slate-400 uppercase tracking-wide">Related Event</p>
                                <a href="{{ route(''events.show'', $message->event->id) }}" class="text-cyan-400 hover:text-cyan-300 font-semibold underline">
                                    {{ $message->event->title }}
                                </a>
                            </div>
                        </div>
                    @endif
                </div>
            </div>

            <!-- Message Content -->
            <div class="bg-slate-800/50 rounded-xl p-6 border border-slate-700">
                <h3 class="text-lg font-semibold text-cyan-300 mb-3">Message</h3>
                <p class="text-slate-300 leading-relaxed whitespace-pre-line">{{ $message->message }}</p>
            </div>

            <!-- Admin Response Section -->
            @if($message->admin_response)
                <div class="mt-6 bg-green-900/20 rounded-xl p-6 border border-green-700/50">
                    <div class="flex items-center gap-3 mb-3">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-green-400" viewBox="0 0 20 20" fill="currentColor">
                            <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/>
                        </svg>
                        <h3 class="text-lg font-semibold text-green-300">Admin Response</h3>
                    </div>
                    <p class="text-slate-200 leading-relaxed mb-3 whitespace-pre-line">{{ $message->admin_response }}</p>
                    <p class="text-xs text-slate-400">
                        Responded by <span class="font-semibold text-slate-300">{{ $message->admin->name ?? ''Admin'' }}</span> 
                        on {{ $message->admin_responded_at->format(''M d, Y - h:i A'') }}
                    </p>
                </div>
            @endif
        </div>

        <!-- Admin Response Form (Only if user is admin or organizer) -->
        @if(auth()->user() && (auth()->user()->role === ''admin'' || auth()->user()->role === ''organizer''))
            <div class="bg-slate-900 rounded-3xl p-8 border border-cyan-400/30 shadow-2xl">
                <h2 class="text-2xl font-bold text-cyan-300 mb-6">
                    {{ $message->admin_response ? ''Update Response'' : ''Send Response'' }}
                </h2>

                <form action="{{ route(''support.respond'', $message->id) }}" method="POST" class="space-y-6">
                    @csrf
                    
                    <div>
                        <label for="admin_response" class="block text-sm font-semibold uppercase text-cyan-400 mb-2 tracking-wider">
                            Response Message
                        </label>
                        <textarea id="admin_response" 
                                  name="admin_response" 
                                  rows="6" 
                                  required
                                  placeholder="Type your response here..."
                                  class="w-full px-5 py-3 bg-slate-800 border border-slate-700 rounded-xl text-white focus:outline-none focus:ring-2 focus:ring-sky-500 focus:border-sky-500 transition-all placeholder-slate-500">{{ old(''admin_response'', $message->admin_response) }}</textarea>
                        @error(''admin_response'')
                            <p class="text-red-400 text-sm mt-2 font-medium">{{ $message }}</p>
                        @enderror
                    </div>

                    <div>
                        <label for="status" class="block text-sm font-semibold uppercase text-cyan-400 mb-2 tracking-wider">
                            Update Status
                        </label>
                        <select id="status" 
                                name="status" 
                                required
                                class="w-full px-5 py-3 bg-slate-800 border border-slate-700 rounded-xl text-white focus:outline-none focus:ring-2 focus:ring-sky-500 focus:border-sky-500 transition-all">
                            <option value="open" {{ old(''status'', $message->status) == ''open'' ? ''selected'' : '''' }}>Open</option>
                            <option value="in_progress" {{ old(''status'', $message->status) == ''in_progress'' ? ''selected'' : '''' }}>In Progress</option>
                            <option value="resolved" {{ old(''status'', $message->status) == ''resolved'' ? ''selected'' : '''' }}>Resolved</option>
                        </select>
                        @error(''status'')
                            <p class="text-red-400 text-sm mt-2 font-medium">{{ $message }}</p>
                        @enderror
                    </div>

                    <div class="flex justify-end">
                        <button type="submit" 
                                class="inline-flex items-center gap-2 bg-gradient-to-r from-sky-500 to-blue-600 hover:from-sky-400 hover:to-blue-500 px-8 py-3 rounded-xl text-white font-bold shadow-lg transition-all transform hover:scale-105">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                                <path d="M10.894 2.553a1 1 0 00-1.788 0l-7 14a1 1 0 001.169 1.409l5-1.429A1 1 0 009 15.571V11a1 1 0 112 0v4.571a1 1 0 00.725.962l5 1.428a1 1 0 001.17-1.408l-7-14z"/>
                            </svg>
                            Send Response
                        </button>
                    </div>
                </form>
            </div>
        @endif
    </div>
</div>
@endsection
